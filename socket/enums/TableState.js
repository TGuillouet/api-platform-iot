/**
 * Represent all the possible states of a table
 */
const TableState = {
	NOT_TAKEN: 'not_taken',
	TAKEN: 'taken',
	PROCESSING: 'process',
	PROCESSED: 'processed'
};

module.exports = TableState;
